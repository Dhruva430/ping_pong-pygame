[tool.poetry]
name = "poetry install pygame"
version = "0.1.0"
description = ""
authors = ["Dhruva430 <rdhruva12345@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pygame = "^2.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
